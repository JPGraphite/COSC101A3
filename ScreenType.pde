/**
 * Enum representing different types of screens in the game.
 * Starting screen, paused screen, next level screen, and completed screen.
 * This has been set to ensure only one type of screen can be active at
 * any given time.
 */

enum ScreenType {
  STARTING, 
  PAUSED, 
  NEXT_LEVEL,
  COMPLETED; 
}